---
import BaseHead from '../components/BaseHead.astro';
---

<html lang="zh-CN">
	<head>
		<BaseHead
			title="即将离开本站"
			description="站外链接确认页：在浏览器内校验对称 MAC（hash）与可选的 Ed25519（sig）后，可选择继续访问目标地址。"
			disableTikkun
		/>
		<meta name="robots" content="noindex, nofollow" />
	</head>
	<body class="out-of-site-body">
		<main class="out-of-site-main">
			<h1 class="out-of-site-title">即将离开本站</h1>
			<p id="out-of-site-status" class="out-of-site-status" hidden></p>
			<p id="out-of-site-detail" class="out-of-site-detail" hidden>
				<code id="out-of-site-url" class="out-of-site-url"></code>
			</p>
			<p class="out-of-site-actions">
				<a id="out-of-site-proceed" class="primary-button" href="#" hidden>继续访问</a>
				<a class="out-of-site-link" href="/">返回首页</a>
			</p>
		</main>
		<script>
			import { runOutOfSitePage } from '../lib/out-of-site-verify-client.ts';

			void runOutOfSitePage({
				ed25519SpkiB64: import.meta.env.PUBLIC_OUT_OF_SITE_ED25519_SPKI_B64 ?? '',
				outOfSiteLinkHmacKey: import.meta.env.PUBLIC_OUT_OF_SITE_LINK_HMAC_KEY ?? '',
			});
		</script>
	</body>
</html>

<style>
	.out-of-site-body {
		margin: 0;
		min-height: 100dvh;
		box-sizing: border-box;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		padding: var(--page-gutter, 16px);
		font-family: var(--font-ui, system-ui, sans-serif);
		background: var(--site-header-bg);
		color: var(--site-fg);
	}

	.out-of-site-main {
		position: relative;
		z-index: 0;
		box-sizing: border-box;
		width: min(100%, 28rem);
		margin: 0;
		padding: clamp(1.5rem, 4vw, 2.25rem);
		background: var(--site-bg);
		border: 1px solid var(--site-border-strong);
	}

	.out-of-site-title {
		margin: 0 0 0.875rem;
		font-size: clamp(1.25rem, 2.8vw, 1.55rem);
		font-weight: 700;
		letter-spacing: -0.02em;
		line-height: 1.25;
	}

	.out-of-site-status {
		margin: 0;
		line-height: 1.65;
		font-size: 0.975rem;
	}

	.out-of-site-status.is-warning {
		color: var(--site-danger);
		font-weight: 700;
	}

	.out-of-site-detail {
		margin: 1rem 0 0;
	}

	.out-of-site-url {
		display: block;
		box-sizing: border-box;
		width: 100%;
		margin: 0;
		padding: 0.45em 0.55em;
		border: 1px solid var(--site-border);
		border-radius: 6px;
		font-family: var(--font-code);
		font-size: 0.875em;
		line-height: 1.45;
		color: var(--site-fg);
		word-break: break-all;
		background: var(--site-surface-muted);
	}

	.out-of-site-actions {
		margin: 1.375rem 0 0;
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem 1.25rem;
		align-items: center;
	}

	.out-of-site-actions .primary-button {
		text-decoration: none;
	}

	.out-of-site-actions .primary-button:hover,
	.out-of-site-actions .primary-button:active {
		text-decoration: none;
	}

	.out-of-site-actions .primary-button.is-pending,
	.out-of-site-actions .primary-button[aria-disabled='true'] {
		opacity: 0.5;
		pointer-events: none;
		cursor: not-allowed;
		transform: none;
	}

	.out-of-site-link {
		color: var(--site-accent);
		text-decoration: underline;
		text-underline-offset: 0.2em;
		font-size: 0.925rem;
	}

	.out-of-site-link:hover {
		color: var(--site-accent-hover, var(--site-accent));
	}
</style>
