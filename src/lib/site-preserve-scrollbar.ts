/**
 * @deprecated Import from `@umamichi-ui/common-components/overlay` instead.
 * Kept as a thin re-export for non-bundled site scripts that still use this path.
 */
export {
  acquirePreservedScrollbar,
  releasePreservedScrollbar,
} from '@umamichi-ui/common-components/overlay';
