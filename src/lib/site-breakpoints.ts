/**
 * Site layout breakpoints — keep in sync with CSS in site-layout.css / global.css:
 *   md: 48rem
 *   xl: 80rem (article fixed sidebars / desktop TOC)
 */
export const SITE_MD_MIN_MQ = '(min-width: 48rem)';
export const SITE_XL_MIN_MQ = '(min-width: 80rem)';
