1. 任何页面都要以内容、语义为中心，不要添加没有语义的视觉元素
2. 如果不是内容、语义所必须，不要在内容区设置卡片。如果要分隔不同的区域，请使用灰色的细线分隔
3. 不要使用渐变和阴影。对于需要用户确认或专注修改设置的 popup 等一定需要与其他内容分层的内容，请将背景整体暗化
4. 不要直接使用矩形，使用圆角矩形
5. 控件的大小不应该有“设计感”地过大
5. 使用白色和灰色作为主要颜色，使用 aqua 色作为突出和品牌识别用的主题色